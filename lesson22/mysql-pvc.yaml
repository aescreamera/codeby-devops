apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mysql-pvc
  namespace: dev

spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  volumeName: mysql-pv
  resources:
    requests:
      storage: 2Gi
